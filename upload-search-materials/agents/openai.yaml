interface:
  display_name: "天猫搜推素材上传"
  short_description: "前端优先配置、审查并安全发布搜推素材"
  default_prompt: "Use $upload-search-materials. Start or resume the managed interaction UI before requesting structured business inputs in chat, continue only from its validated handoff, and stop at exact approval and production safety gates."
