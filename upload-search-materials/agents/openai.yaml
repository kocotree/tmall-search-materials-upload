interface:
  display_name: "天猫搜推素材自动化"
  short_description: "审计、补采、审批并安全发布天猫搜推素材"
  default_prompt: "Use $upload-search-materials to prepare the runtime, open the setup UI before requesting business inputs, then continue from its validated handoff and stop at explicit safety gates."
