# automated-media-upload--skill 协作说明

## 项目定位

本项目是一个面向天猫搜推素材任务的 Codex Skill。它通过本地交互页面组织商品配置、素材源选择、文件夹匹配、人工选图、确定性坑位编排、文案确认、dry-run、审批和最终上传。

项目不是通用 NAS 客户端，也不是无人值守发布服务。NAS 只作为只读素材来源；生产写入、发布授权和关键业务选择必须遵守 Skill 中的人工确认边界。

当前架构以跨平台为目标：

- Windows 使用本机路径、映射盘或 UNC，并通过受控 PowerShell/Explorer 桌面流程处理原生交互。
- macOS 使用本机路径或 `/Volumes/...` 挂载点，并通过 Finder/AppleScript 系统流程处理原生交互。
- 跨电脑业务数据使用稳定的 `source_id + relative_path`，不得依赖固定盘符、用户名或绝对挂载路径。

## 主要目录

- `SKILL.md`：Skill 的主要业务契约、执行边界和交互要求。
- `src/upload_search_materials/`：Python 主程序。
  - `interaction/`：Flask 本地交互页面、会话状态、前端资源和原生目录选择器。
  - `browser/`：千牛/素材中心浏览器自动化能力。
  - `runtime_config.py`：每台电脑的运行时路径和图片源绑定。
  - `nas_sources.py`：NAS 来源定义、挂载身份检查、辅助连接和安全目录浏览。
  - `folder_index.py`、`asset_index.py`：只读素材目录和素材索引。
  - `deterministic_slot_planning.py`：不依赖 AI 的确定性坑位编排。
- `config/`：示例配置、NAS 共享定义和本机配置入口。不得提交密码、Token、Cookie 或 NAS 凭据。
- `references/`：操作说明、数据结构、错误处理和决策边界。
- `scripts/`：Windows/macOS/Linux 的启动、注册和诊断入口。
- `tests/`：单元测试、接口测试、工作流测试及脱敏 fixtures。
- `openspec/changes/`：已提出或实施中的变更规格。
- `runs/`：本地任务运行数据，不作为源代码修改。
- `.codegraph/`：代码关系索引；存在时应先使用 CodeGraph 理解调用链。

仓库中遗留的 `upload-search-materials/` 是旧架构目录。除非用户明确要求迁移某项旧能力，否则不得恢复、继续扩展或把新代码写回该目录。

## 开发原则

1. 交互性测试由用户执行。Codex 只负责代码层面的单元测试、接口测试、静态检查和非交互回归，不主动点击真实页面、不弹出 Finder/Explorer/原生目录窗口、不执行真实登录、上传或发布。交付时必须明确列出仍需用户手工验收的交互步骤。
2. 对是否需要增加的功能、字段、页面、依赖、配置或流程存在不确定时，必须先向用户澄清。用户没有明确提出的新增内容，不得自行实现；只有用户确认后才能加入。
3. 修改现有 Skill 时必须先完整读取适用的 `SKILL.md`。若仓库根目录存在 `.codegraph/`，理解或定位代码时必须先使用 CodeGraph，再使用 `rg` 或直接读取文件。
4. 变更必须基于当前新架构完成，优先复用现有运行时配置、交互页面、会话协议、错误码和测试设施。不得因为旧实现可用就直接恢复旧目录树。
5. 保持 Windows 与 macOS 行为对等。平台差异应收敛到小型适配器中，公共业务逻辑只维护一份。
6. 所有 NAS 检测默认只读。未经用户明确操作不得自动挂载、映射网络盘或弹出认证窗口；不得读取、传递或保存 NAS 凭据。
7. 素材源配置分为逻辑身份和本机绑定。任务数据保存 `source_id + relative_path`；`Y:/Z:`、UNC 和 `/Volumes/...` 只属于每台电脑的本机配置或运行时状态。
8. NAS 子路径必须拒绝绝对路径、盘符、`..` 和解析后的目录逃逸。配置阶段不得扫描或打开图片；只有明确的素材执行阶段可以读取任务所需原图。
9. 不得擅自改变人工确认边界、审批规则、上传行为、AI 使用范围或确定性坑位编排。涉及生产写入的行为必须保持精确清单、revision 和 SHA-256 绑定。
10. 保留用户已有修改和未跟踪文件。不得使用破坏性 Git 命令；不得未经要求清理 stash、重置分支、提交、推送或创建 PR。

## 测试与交付

- 新功能至少覆盖正常、取消、超时、权限不足、无效输入和无副作用路径中与改动相关的场景。
- 优先运行针对性测试，再运行受影响模块回归。若全量测试存在基线失败，应区分本次回归结果与既有失败，不得把未通过描述为通过。
- 原生窗口、Finder/Explorer、真实 NAS 登录、千牛登录和最终视觉交互由用户手工验收；Codex 不代替用户执行。
- 交付前运行适用的语法检查、`git diff --check` 和 Skill 校验，并说明当前分支、未提交文件和未完成的人工验收项。
