# automated-media-upload--skill 协作说明

## 项目定位

本项目是一个面向天猫搜推素材任务的 Codex Plugin。Plugin 内包含 `upload-search-materials` 与 `maintain-team-folder-index` 两个独立 Skill：前者通过本地交互页面组织商品配置、素材源选择、文件夹匹配、人工选图、确定性坑位编排、文案确认、dry-run、审批和最终上传；后者负责去中心化团队文件夹索引的校验、同步、显式增量更新和不可变快照发布。

项目不是通用 NAS 客户端，也不是无人值守发布服务。NAS 只作为只读素材来源；生产写入、发布授权和关键业务选择必须遵守 Skill 中的人工确认边界。

当前 `win` 分支仅面向 Windows 11 开发和交付，不再包含其他操作系统的运行入口、平台适配、兼容性维护或行为对等要求。Windows 使用本机路径、映射盘或 UNC，并通过受控 PowerShell/Explorer 桌面流程处理原生交互。新增或修改的代码、脚本、文档和测试都必须以 Windows 11 为唯一目标平台。

跨电脑业务数据仍使用稳定的 `source_id + relative_path`，不得依赖固定盘符、用户名或绝对 UNC 根路径。

## 主要目录

- `.codex-plugin/plugin.json`：Plugin 的安装、发现和界面元数据。
- `skills/`：Plugin 对外提供的两个 Skill；`upload-search-materials` 入口加载根目录 canonical `SKILL.md`。
- `SKILL.md`：上传 Skill 的 canonical 业务契约、执行边界和交互要求。
- `src/upload_search_materials/`：Python 主程序。
  - `interaction/`：Flask 本地交互页面、会话状态、前端资源和原生目录选择器。
  - `browser/`：千牛/素材中心浏览器自动化能力。
  - `runtime_config.py`：每台电脑的运行时路径和图片源绑定。
  - `nas_sources.py`：NAS 来源定义、挂载身份检查、辅助连接和安全目录浏览。
  - `folder_index.py`、`asset_index.py`：只读素材目录和素材索引。
  - `deterministic_slot_planning.py`：不依赖 AI 的确定性坑位编排。
- `config/`：示例配置、NAS 共享定义和本机配置入口。不得提交密码、Token、Cookie 或 NAS 凭据。
- `references/`：操作说明、数据结构、错误处理和决策边界。
- `scripts/`：Windows 启动、注册和诊断入口；不得新增其他操作系统的启动脚本。
- `tests/`：单元测试、接口测试、工作流测试及脱敏 fixtures。
- `openspec/changes/`：已提出或实施中的变更规格。
- `runs/`：本地任务运行数据，不作为源代码修改。
- `.codegraph/`：代码关系索引；存在时应先使用 CodeGraph 理解调用链。

注意：主要目录在开发过程中的时候是需要持续维护的。

仓库中遗留的 `upload-search-materials/` 是旧架构目录。除非用户明确要求迁移某项旧能力，否则不得恢复、继续扩展或把新代码写回该目录。

## Plugin 发布与测试流程
1. 当任务涉及 Plugin 版本号、发布、提交后推送、GitHub/Marketplace 分发、安装/升级/移除/重新添加、安装缓存核验、测试端更新、工作台新旧版本切换、既有会话在新版本上恢复，或排查“代码已更新但测试端未生效”时，必须在执行相关操作前完整读取 `harness-docs/Plugin 发布与测试流程.md`。
2. 仅进行业务逻辑开发、局部文档修改、代码阅读或不涉及发布环境的本地测试时，无需加载`harness-docs/Plugin 发布与测试流程.md`文档；如果任务随后扩展到上述任一发布或测试端场景，应立即暂停相关操作并先读取。'
3. 如果安装步骤有变化的时候，请参考以及维护文档：install.md

## 开发原则

1. 交互性测试由用户执行。Codex 只负责代码层面的单元测试、接口测试、静态检查和非交互回归，不主动点击真实页面、不执行真实登录、上传或发布。交付时必须明确列出仍需用户手工验收的交互步骤。
2. 本任务属于确定的业务工作流。仅保留工作台相关交互逻辑；在 Codex 对话链路中，应当尽量避免输出命令执行审批卡片、终端命令审批卡、Command approval card、Approval request、confirmation boundary 等各类审批确认弹窗，无需重复请求人工确认。例外条件：仅当出现业务流程之外的突发异常，需要执行非正常业务路径时，才触发用户授权与审批确认。
3. 对是否需要增加的功能、字段、页面、依赖、配置或流程存在不确定时，必须先向用户澄清。用户没有明确提出的新增内容，不得自行实现；只有用户确认后才能加入。
4. 修改现有 Skill 时必须先完整读取适用的 `SKILL.md`。若仓库根目录存在 `.codegraph/`，理解或定位代码时必须先使用 CodeGraph，再使用 `rg` 或直接读取文件。
5. 所有变更均需基于当前新架构开展。若需求变更与新架构已完成能力存在重合，应优先复用新架构现有运行时配置、业务代码、脚本、交互页面、会话协议、错误码及测试设施，避免重复开发。可参考旧实现的业务逻辑片段进行适配改造，但禁止因旧实现功能可用，直接回迁、恢复旧架构完整目录树 / 旧模块。。
6. 当前 `win` 分支只针对 Windows 11 开发、测试和验收。运行入口、路径处理、桌面交互和测试应采用 Windows 原生路径、PowerShell/Explorer 桌面流程及 Windows 运行身份，不得恢复其他操作系统的适配。
7. 除共享文件夹索引所在的子目录外，所有 NAS 检测默认只读。未经用户明确操作不得自动挂载、映射网络盘或弹出认证窗口；不得读取、传递或保存 NAS 凭据。
8. 素材源配置分为逻辑身份和本机绑定。任务数据保存 `source_id + relative_path`；Windows 盘符、映射盘和 UNC 根路径只属于每台电脑的本机配置或运行时状态。
9. NAS 子路径必须拒绝绝对路径、盘符、`..` 和解析后的目录逃逸。配置阶段不得扫描或打开图片；只有明确的素材执行阶段可以读取任务所需原图。
10. 不得擅自改变人工确认边界、审批规则、上传行为、AI 使用范围或确定性坑位编排。涉及生产写入的行为必须保持精确清单、revision 和 SHA-256 绑定。
11. 保留用户已有修改和未跟踪文件。不得使用破坏性 Git 命令；不得未经要求清理 stash、重置分支、提交、推送或创建 PR。
12. 交付前运行适用的语法检查、`git diff --check` 和 Skill 校验，并说明当前分支、未提交文件和未完成的人工验收项。

## 前端交互设计原则
1. 用户视角导向：站在终端用户视角设计交互页面，页面需清晰传达当前所处状态、用户可执行的操作，让用户明确知道当下应该做什么、下一步可以做什么，减少理解成本。
2. 异常友好提示：业务异常场景必须对用户给出明确、易懂的反馈提示，例如素材源重复、素材源为空、参数非法、操作失败等场景；提示信息面向业务使用者，避免晦涩技术术语。
3. 区分开发态与用户态展示：保持整体页面简洁干净，严禁将开发调试类提示、内部技术配置、底层报错堆栈、后端原始字段直接暴露给前端用户。调试日志、内部配置仅允许保留在开发环境，生产环境对用户完全屏蔽。

## skill设计原则
1. 调用本 Skill 的 Agent（通常为 Codex）可感知并追踪 Skill 的完整执行步骤，配置与执行流程对该 Agent 完全透明。
2. 本 Skill 具备自进化能力：调用方 Agent（通常为 Codex）具备修改、迭代本 Skill 自身逻辑的能力，可基于现场问题对 Skill 进行自我调整优化（防止下次出现同样的问题）。执行过程中若遇到异常或问题，可以收集分析任务执行失败时的轨迹，解决当前问题。
3. 以整体流程为纲，来把控、约束局部子流程；局部流程要服从整体目标，不能只盯着局部忽略全局


## 交付
1. 第一版交付优先打通完整全流程，保障端到端链路可正常运行，完成基础可用版本。
2. 基于第一版可用基线，再进行第二版迭代，持续做细节、体验与性能优化。
3. 目标交付用户不具备 AI 与编程相关技术背景。Skill 执行过程中，应避免抛出技术底层问题让用户确认；面向用户的交互确认需站在业务功能视角，使用业务语言引导用户做选择与确认，禁止暴露技术细节、底层报错、内部参数等技术类信息。
