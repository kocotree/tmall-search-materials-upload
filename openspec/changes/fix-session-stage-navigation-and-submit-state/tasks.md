## 1. 历史会话启动与导航状态

- [x] 1.1 为仅带 `session_id` 打开历史任务时固定落到阶段 01 的现状增加失败回归测试
- [x] 1.2 重构启动顺序，在激活初始阶段前读取会话快照并校验 `session.current_stage`
- [x] 1.3 实现无效或缺失 `current_stage` 的只读回退与可恢复警告，不修改历史会话
- [x] 1.4 使用会话快照一次性同步全部导航项的 revision/status，删除逐阶段访问后才更新的依赖
- [x] 1.5 为已完成、已提交和处理中阶段增加明确锁定原因及“进入当前阶段”入口

## 2. 初始化与水合无副作用

- [x] 2.1 扩展隐藏 JSON 控件写入接口，支持静默水合并在新旧规范化值相同时跳过事件
- [x] 2.2 将服务端输入水合、图片审查默认决定、文件夹/画廊同步和坑位默认编排改为静默初始化
- [x] 2.3 确保只有用户点击、选择、拖拽或文本输入才标记 dirty 并调度自动保存
- [x] 2.4 为图片审查和坑位编排增加“只打开不产生 revision”的单元与集成测试
- [x] 2.5 比较历史任务打开前后的 revision、input SHA-256、handoff 和决定内容，验证初始化幂等

## 3. 保存与提交竞态

- [x] 3.1 在 UI 状态层增加单飞持久化请求和待处理保存/提交意图模型
- [x] 3.2 保存进行期间收到提交时显示排队状态，并在保存成功后用最新 revision 与表单值继续提交
- [x] 3.3 合并重复保存请求但保留提交的更高优先级，防止无意义 revision 和重复 handoff
- [x] 3.4 保存失败时保留 dirty 数据、暂停待处理提交并展示可恢复错误
- [x] 3.5 阶段或 session 切换时取消旧的待处理意图，并保持 generation/request identity 隔离
- [x] 3.6 为排队、合并、失败、服务端校验失败和跨阶段取消增加确定性状态测试

## 4. 交互反馈与兼容性

- [x] 4.1 为保存中、提交排队、正在提交、成功、校验失败和传输失败增加明确页面反馈
- [x] 4.2 保持现有阶段 API、`session.json`、input/handoff/result Schema 和服务端锁定规则兼容
- [x] 4.3 验证 completed/ready_for_agent/processing 阶段不能被前端重新启用或覆盖
- [x] 4.4 更新前端状态测试，覆盖快速连续点击时操作不会被静默忽略

## 5. 验收与文档

- [x] 5.1 使用会话 `20260725_095720` 做真实浏览器恢复测试，确认刷新后直接进入第四阶段
- [x] 5.2 确认全部导航状态与会话文件一致，且只查看页面不会创建新 revision
- [x] 5.3 在自动保存期间点击“提交给 Agent”，确认最终仅产生一次有效 handoff 并显示全过程状态
- [x] 5.4 更新 `SKILL.md`、交互操作说明和 `test_plan.md` 中的历史任务恢复及提交竞态验收步骤
- [x] 5.5 运行 Python、Node、浏览器回归、Skill validator、OpenSpec strict 和 `git diff --check`
