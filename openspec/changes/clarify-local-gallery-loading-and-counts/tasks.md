## 1. 计数模型与兼容字段

- [x] 1.1 为 gallery job 进度和完成结果增加发现路径、计划检查、已检查、检查失败、内容重复、最终候选和待处理计数字段
- [x] 1.2 在图片检查循环中按商品维护 SHA-256 已见集合，并分别累计唯一候选与内容重复数量
- [x] 1.3 为每商品输出与批次相同口径的检查、失败、重复和最终候选统计
- [x] 1.4 在完成写入前校验批次与商品级计数不变量，并以同一最终快照持久化 job 与画廊结果
- [x] 1.5 保留旧 `discovered_count` / `prepared_count` 的兼容读取，但禁止新页面用旧字段表达最终候选数

## 2. 候选文件夹素材计数

- [x] 2.1 定义任务级候选文件夹计数文档及稳定的 folder identity、状态、原始递归数量、时间和错误字段
- [x] 2.2 实现只枚举受支持图片路径和必要元数据的低并发本机计数器，确保不打开、解码、哈希或修改图片
- [x] 2.3 为拒绝访问、路径不可用、超时和取消返回稳定状态，失败不得落成素材数 `0`
- [x] 2.4 在文件夹审查数据/API 中暴露异步计数状态，并允许页面首屏在计数完成前立即展示
- [x] 2.5 将 gallery job 的路径去重数、抽样检查数和最终唯一候选数按文件夹回填到已采用文件夹
- [x] 2.6 明确父子候选文件夹的原始递归计数可以重叠，并保留加载后的本轮有效数量作为独立字段

## 3. 两个独立按钮与本机状态

- [x] 3.1 在文件夹列表下方、候选图片上方新增独立“确认文件夹并加载图片”按钮
- [x] 3.2 将本机按钮绑定到 `prepare-gallery` 本地请求，确保该事件不经过通用阶段提交处理器
- [x] 3.3 在 `folder_review` 和 `gallery_preparing` 步骤隐藏页面底部的阶段提交按钮
- [x] 3.4 仅在 `image_selection` 步骤显示原位置的“确认选图并提交给 Codex”按钮
- [x] 3.5 将本机加载期间所有 Agent/Codex 文案替换为本机扫描、检查和候选生成文案
- [x] 3.6 在每个候选文件夹卡片显示“统计中 / 原始递归素材数 / 素材数未知”，画廊完成后补充本轮有效统计

## 4. 进度与完成状态渲染

- [x] 4.1 运行中显示“发现 N 张，正在检查 X/Y 张”，并单独显示检查失败和内容重复数量
- [x] 4.2 完成后显示“检查 Y 张，失败 F 张，内容重复 D 张，可展示候选 C 张”
- [x] 4.3 在商品标题区域显示每商品的检查上限、实际检查数、重复数、最终候选数和差额原因
- [x] 4.4 修复 `running → completed` 状态切换，使最终结果覆盖最后一次运行消息
- [x] 4.5 验证刷新页面从完成快照重建相同统计，不保留 `已准备 284 张` 等陈旧文本
- [x] 4.6 对旧会话缺少新字段的情况显示明确的“历史进度口径”，不得猜测重复数或最终候选数

## 5. 简单自动化验证

- [x] 5.1 添加接口测试：本机文件夹确认创建 gallery job 但不创建 handoff 或 Agent wait
- [x] 5.2 添加接口测试：只有最终有效选图提交创建唯一 `final_material_selection` handoff
- [x] 5.3 添加计数器单元测试：成功计数、拒绝访问、超时和不打开图片内容
- [x] 5.4 添加计数模型单元测试：固定验证 `检查 300 → 重复 14 → 最终 286` 及商品级求和不变量
- [x] 5.5 添加静态页面/渲染测试：两个按钮的位置、分步可见性和本机文案
- [x] 5.6 添加状态测试：完成快照替换运行快照，刷新后不恢复陈旧进度
- [x] 5.7 只运行与本变更直接相关的快速单元和接口测试，不执行真实 NAS、RaiDrive、Windows picker 或长时间浏览器交互测试

## 6. Skill 文档与用户验收交接

- [x] 6.1 更新 canonical Skill，明确第一个按钮是本机硬编码且不触发 handoff，第二个按钮才交给 Codex
- [x] 6.2 更新数据结构和运维说明，记录文件夹计数、gallery job 新计数字段及旧会话兼容语义
- [x] 6.3 准备简短的人工验收清单，交由用户测试真实 NAS 计数、按钮点击、长时间加载、刷新和中断恢复
- [x] 6.4 在实施交付中明确列出已完成的简单自动化测试和留给用户的复杂交互测试，不代替用户执行后者
