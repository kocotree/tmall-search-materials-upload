## 1. 图片策略与合规分类

- [x] 1.1 扩展运行时图片策略 Schema 和示例文件，加入允许比例、比例容差、3:4 与 1:1 推荐尺寸、推荐尺寸阻断模式、最大图片大小及裁剪输出配置
- [x] 1.2 在任务第四阶段目录保存策略快照与 SHA-256，并在策略缺失或不完整时返回稳定阻断原因
- [x] 1.3 实现统一图片合规分类器，输出两个目标比例的推荐分辨率比较，以及可直传、可裁剪、需要压缩、需要人工处理和不可使用状态
- [x] 1.4 增加分类器单元测试，覆盖 3:4、1:1、容差边界、达到/低于推荐分辨率、文件大小边界与超限、损坏、零尺寸和不支持格式

## 2. 第三阶段预检提示

- [x] 2.1 将原始宽高、1:1/3:4 最大内接尺寸及推荐分辨率比较、原始比例、`size_bytes`、格式化文件大小、合规状态和原因码加入第三阶段候选数据
- [x] 2.2 在图片卡片显示“达到推荐 / 低于推荐”、原始比例、当前大小/大小上限及“可直传 / 可裁剪 / 需要压缩 / 不可使用”标签，并提供对应筛选
- [x] 2.3 仅禁用确定不可用图片的“采用”控件，确认低于推荐尺寸、比例不匹配和文件大小超限图片仍可选择
- [x] 2.4 增加第三阶段静态、交互和后端防篡改测试，覆盖大小格式化、20MB 边界、超限提示以及未实现压缩时不展示压缩按钮

## 3. 第四阶段结果准备

- [x] 3.1 实现第四阶段准备命令，读取第三阶段当前提交 revision、已选图片和任务策略快照
- [x] 3.2 生成包含源 SHA-256、宽高、比例、文件大小、推荐分辨率比较、可选目标比例、处理状态及原因码的第四阶段 review-context
- [x] 3.3 将第四阶段结果绑定第三阶段 revision，并在第三阶段重新提交后将旧结果标记为过期
- [x] 3.4 为历史任务补建第四阶段结果，对不可读源文件逐项记录错误且保留其他图片
- [x] 3.5 增加准备命令、历史任务和 revision 过期测试

## 4. 人工裁剪与派生处理接口预留

- [x] 4.1 定义直接使用、裁剪、仅作候选和排除的结构化决定 Schema
- [x] 4.2 为每张图片计算 1:1 和 3:4 两个最大内接、确定性居中的初始裁剪框
- [x] 4.3 实现固定比例的裁剪框移动与缩放，以及规范化裁剪框、像素裁剪框和目标输出尺寸之间的双向校验
- [x] 4.4 实现任务目录内的高质量派生文件生成，记录源/输出 SHA-256、策略版本和完整来源链
- [x] 4.5 定义不依赖外部服务的可插拔 AI 裁剪建议接口，并提供空实现和测试替身
- [x] 4.6 定义不执行实际压缩的可插拔图片压缩接口，并提供空实现和测试替身，校验输出路径、大小、SHA-256、提供方和版本
- [x] 4.7 增加双比例初始框、边界约束、路径越界、源文件变化、重复生成、压缩接口契约和源文件未修改测试

## 5. 第四阶段可视化页面

- [x] 5.1 用专用图片审查组件替换第四阶段裸露的策略路径、比例容差和决定 JSON 输入框
- [x] 5.2 展示第三阶段已选图片总数、原图预览、宽高、推荐分辨率比较、比例、文件大小、预检状态和原因码
- [x] 5.3 实现 3:4 / 1:1 目标切换、两个比例各自调整状态的保留、原图与裁剪预览并排展示、裁剪框拖动缩放及输出尺寸反馈
- [x] 5.4 实现“直接使用 / 裁剪 / 仅作候选 / 排除”决定和人工调整、最终确认的审计状态
- [x] 5.5 实现尚未扫描、策略阻断、结果过期和源文件不可读页面状态；首版不展示 AI 裁剪或图片压缩操作入口
- [x] 5.6 增加前端交互测试及 1440×900、1024×768 真实浏览器验收

## 6. 第五阶段坑位校验

- [x] 6.1 修改第五阶段素材来源，只允许选择当前第四阶段确认的直接使用或裁剪输出
- [x] 6.2 对每个坑位强制校验 3–9 张、单一输出比例和最大图片大小，并返回稳定数量、`MIXED_ASPECT_RATIO` 和 `IMAGE_SIZE_EXCEEDED` 原因
- [x] 6.3 阻止过期、仅作候选、已排除或未经第四阶段确认的图片进入坑位
- [x] 6.4 增加合规坑位、混合比例、图片大小超限、数量不足/超限和未确认图片测试

## 7. 流程验收与文档

- [x] 7.1 使用任务 `20260725_095720` 当前提交的 9 张图片补建第四阶段结果并完成页面验收
- [x] 7.2 新建隔离任务完成第三阶段宽高/比例/大小预检、第四阶段直传/1:1 人工裁剪/3:4 人工裁剪和第五阶段编排的端到端 dry-run
- [x] 7.3 核对裁剪前后共享盘原图大小、mtime 和 SHA-256 不变，派生文件仅存在于时间戳任务目录
- [x] 7.4 更新 `SKILL.md`、图片要求、数据 Schema、操作说明和 `test_plan.md`
- [x] 7.5 运行完整 pytest、JavaScript 检查、Skill validator 和 OpenSpec strict validation
