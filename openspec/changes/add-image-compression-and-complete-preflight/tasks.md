## 1. 策略与数据契约

- [x] 1.1 扩展任务级图片策略，加入最小宽高、最小/最大字节数、压缩质量区间、输出格式、透明背景色和像素安全上限
- [x] 1.2 更新默认媒体策略为 720×720、200KiB–20MiB、1:1/3:4 及对应推荐分辨率
- [x] 1.3 为完整 `source_inspection`、双比例 `target_assessments` 和 `processed_output` 增加模型及 JSON Schema
- [x] 1.4 增加策略版本、策略 SHA-256、第三阶段 revision 和处理提供方版本的审计字段
- [x] 1.5 为新增稳定原因码及中文恢复文案增加集中定义
- [x] 1.6 添加策略边界、缺失字段、序列化和向后兼容失败测试

## 2. 原图检查与增量缓存

- [x] 2.1 实现只读图片检查器，采集格式、原始字节数、EXIF 纠正宽高、比例、SHA-256 和错误状态
- [x] 2.2 实现 1:1 与 3:4 最大内接尺寸及最小/推荐分辨率判断
- [x] 2.3 实现以规范化路径、文件大小和修改时间为快速键的检查缓存，并保留 SHA-256 最终身份
- [x] 2.4 将完整检查结果接入第三阶段候选生成和最多 100 张候选池持久化
- [x] 2.5 为历史候选增加缺失预检检测和按当前原文件重建流程
- [x] 2.6 为不存在、无权限、损坏、零尺寸、EXIF 旋转、透明通道和共享盘缓存命中增加测试

## 3. 第三阶段合规分类

- [x] 3.1 重构图片分类器，使其统一读取任务策略并区分硬门槛与推荐警告
- [x] 3.2 实现小于 200KiB、无法产生 720×720 输出、格式不支持及读取失败的阻断逻辑
- [x] 3.3 实现大于 20MiB 时根据压缩提供方实际可用性决定是否允许采用
- [x] 3.4 实现“可直接使用 / 可裁剪 / 需要压缩 / 裁剪并压缩 / 不可使用”的确定性结论
- [x] 3.5 为 200KiB、20MiB、720 像素、比例容差和推荐尺寸临界值增加参数化测试

## 4. Pillow 压缩提供方

- [x] 4.1 在现有 `ImageCompressionProvider` 契约下实现并注册 `PillowImageCompressionProvider`
- [x] 4.2 实现 EXIF 纠正、可选裁剪、透明背景合成、RGB 转换和任务本地 JPEG 输出
- [x] 4.3 实现从最高质量开始的质量搜索，保留满足上限的最高可用质量
- [x] 4.4 实现最低质量仍超限时的受控缩放，并保证输出不低于最小宽高
- [x] 4.5 实现输出不足 200KiB、目标不可达、像素超限和不支持格式的明确失败结果
- [x] 4.6 实现派生路径边界、稳定输出身份、同输入复用及处理前后源文件身份核验
- [x] 4.7 为仅压缩、裁剪后无需压缩、裁剪并压缩、透明 PNG、源文件不变和路径越界增加测试

## 5. 第三阶段界面

- [x] 5.1 在图片卡片固定展示原图尺寸、原始比例、原图文件大小和允许大小范围
- [x] 5.2 同时展示 1:1/3:4 最大可裁剪尺寸、最小尺寸状态和推荐分辨率状态
- [x] 5.3 展示处理建议与稳定原因，确保原图属性和预计输出属性使用不同标签
- [x] 5.4 对硬阻断候选禁用“采用”，对压缩能力可用的超限候选保留“采用”
- [x] 5.5 为检查中、历史数据待重建、检查失败和检查完成增加可恢复页面状态
- [x] 5.6 更新前端状态测试和第三阶段服务端篡改提交测试

## 6. 第四阶段处理与对比界面

- [x] 6.1 扩展处理决定为直接使用、裁剪、压缩、裁剪并压缩、仅作候选和排除
- [x] 6.2 将人工确认裁剪接入统一处理流水线，并按裁剪结果实际大小决定是否继续压缩
- [x] 6.3 在第四阶段并列展示原图和处理后文件的大小、尺寸、比例、格式、质量及处理动作
- [x] 6.4 记录完整源到输出来源链、压缩率、策略身份、第三阶段 revision 和用户确认时间
- [x] 6.5 处理失败时保留可审计错误且禁止确认；第三阶段 revision 变化时将旧输出标记过期
- [x] 6.6 增加第四阶段 API、持久化、恢复、原图/输出对比和浏览器交互测试

## 7. 第五阶段最终校验

- [x] 7.1 修改坑位编排，只接收当前 revision 已确认的直接、裁剪、压缩或裁剪并压缩输出
- [x] 7.2 在服务端重新读取实际输出并校验 SHA-256、200KiB–20MiB、最小宽高和允许比例
- [x] 7.3 保持并验证每坑 3–9 张和坑位内比例一致，显示实际数量及比例组成
- [x] 7.4 为文件替换、元数据篡改、过期 revision、大小越界、尺寸不足和混合比例增加阻断测试
- [x] 7.5 增加从第三阶段选择到第四阶段处理再到第五阶段坑位的端到端测试

## 8. 文档与验收

- [x] 8.1 更新 `SKILL.md`，明确原图预检、压缩能力探测、人工确认和阶段恢复流程
- [x] 8.2 更新图片要求、数据 Schema、操作指南及错误处理参考文档
- [x] 8.3 更新 `test_plan.md`，补充边界值、历史任务重建、共享盘源文件保护和压缩验收
- [x] 8.4 运行 Python、Node、Skill validator 和 OpenSpec strict 全量校验
- [x] 8.5 使用历史任务重建第三阶段并确认页面显示真实原图大小与比例
- [x] 8.6 使用至少一张大于 20MiB 图片完成第三至第五阶段真实浏览器验收并保存测试证据
