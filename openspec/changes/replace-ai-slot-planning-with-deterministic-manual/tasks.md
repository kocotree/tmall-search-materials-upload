## 1. 分支基线与冲突边界

- [x] 1.1 记录 `codex/non-ai-slot-planning` 分支、工作树、历史 session、独立图片适用性阶段和坑位处理当前三页面基线
- [x] 1.2 标记活动变更中独立图片适用性页面、AI 默认编排、AI/人工唯一草稿和三子页面与本变更冲突的要求
- [x] 1.3 增加确定性策略 ID、版本和 SHA-256，并建立相同输入产生相同结果的契约测试
- [x] 1.4 明确新任务不创建坑位 Agent request，但继续允许文案 Agent request

## 2. 素材选择阶段自动预检与坑位提示

- [x] 2.1 定义 selected-asset preflight snapshot，并绑定素材选择 revision、策略 SHA、源身份和检测提供方版本
- [x] 2.2 复用现有检查器，只增量检测采用图片的可读性、SHA、重复、格式、大小、尺寸、比例可行性和压缩能力
- [x] 2.3 将原图信息、`1:1 / 3:4` 评估、待检测和不可用原因回显到素材选择卡
- [x] 2.4 将逐商品后台 `missing_materials` 可靠贯通到素材选择页面
- [x] 2.5 按采用决定、预检状态和源 SHA-256 计算可用唯一图片数
- [x] 2.6 实现 `min(missing_slots, floor(N/3))` 的完整坑位数、均衡预览和补满差额计算
- [x] 2.7 覆盖 0、1、2、3、5、6、8、9、14、20 张及缺失坑位为 0 的边界
- [x] 2.8 显示缺失坑位、已选/可用数量、预计坑位分配、补满还差数量和容量上限
- [x] 2.9 允许保存 1–2 张选择草稿，但禁用提交并分别提示还差 2/1 张
- [x] 2.10 将主动作改为“确认选择并自动编排坑位”，预检未完成或任一提交商品少于 3 张时阻断
- [x] 2.11 证明预检不生成派生图片、不遍历未采用共享盘素材，并增加缓存失效测试
- [x] 2.12 增加选图变化后逐卡更新、重复素材不计数和窄屏布局测试

## 3. 确定性坑位编排器

- [x] 3.1 从现有规则草稿生成代码中抽取独立的确定性编排模块，停止把结果建模为规则兜底
- [x] 3.2 实现用户 `selection_order` 优先、来源文件夹内顺序稳定和来源轮询交错
- [x] 3.3 实现最大化完整坑位数及 3–9 张均衡分配
- [x] 3.4 实现缺失坑位容量限制和 `unused_assets[]`，保留所有未编排素材及原因
- [x] 3.5 实现 `3:4 / 1:1` 公共可行性硬校验和可解释字典序评分
- [x] 3.6 实现无公共比例时的稳定交换/移动，以及无法修复时退回候选池
- [x] 3.7 生成稳定 slot ID、比分组成、策略身份、输入 revision 和分配摘要
- [x] 3.8 增加 9 张在缺 1/2/3 坑时的 `9`、`5+4`、`3+3+3` 回归测试
- [x] 3.9 增加来源交错、比例同分、压缩惩罚、裁剪保留率、容量溢出和不可分组测试
- [x] 3.10 增加重复运行字节级等价、策略变化失效和不同文件枚举顺序不影响结果测试

## 4. 素材选择提交与唯一当前草稿

- [x] 4.1 在素材选择合法提交后同步运行确定性编排器并创建 `current-slot-plan.json`
- [x] 4.2 将新草稿来源限制为 `deterministic`、`manual`、`manual_override`
- [x] 4.3 停止新任务的坑位 Agent request 发现、领取、重试、响应载入和提示词复制
- [x] 4.4 确定性编排失败时保留当前草稿或空状态，并返回可人工处理的原因
- [x] 4.5 实现显式“重新自动编排”及影响提示、精确 revision 校验和多标签页冲突
- [x] 4.6 保留人工加删坑位、加删图片、调序、改比例和不完整草稿保存
- [x] 4.7 保持确认时每坑 3–9 张、同商品、统一比例和素材跨坑唯一的服务端校验
- [x] 4.8 增加素材选择提交幂等、自动草稿审计、人工覆盖、重新编排和迟到旧请求无效测试
- [x] 4.9 移除新任务独立 `image_review` 的导航、提交和二次候选决定，并使 `slots_copy` 直接依赖素材选择
- [x] 4.10 将兼容的历史 suitability snapshot 迁移为 selected-asset preflight 数据，不删除旧阶段文件

## 5. 坑位处理阶段两子页面

- [x] 5.1 将素材选择后的坑位处理导航收敛为“图片裁剪和压缩”“AI 生成标题和描述”两个子页面
- [x] 5.2 删除新任务的独立编排标签、AI 编排按钮、置信度、状态和恢复提示词
- [x] 5.3 在第一页顶部显示自动坑位摘要、编排依据、未使用候选池和人工编辑器
- [x] 5.4 未确认坑位时隐藏或禁用裁剪区，确认精确 plan revision 后在同页展开裁剪
- [x] 5.5 保持共享候选池同步、图片单坑唯一、每批最多 30 张和换一批
- [x] 5.6 坑位成员、顺序或比例变化时撤销确认并准确失效受影响输出与文案
- [x] 5.7 保持可视化 `3:4 / 1:1` 裁剪、键盘操作、预览、原图直出和压缩确认
- [x] 5.8 只有全部实际输出复核通过后才解锁第二页
- [x] 5.9 增加两页面导航、同页递进门禁、手工编辑、候选换批和窄屏浏览器测试

## 6. AI 文案边界、阶段编号与历史迁移

- [x] 6.1 限制文案 Agent 请求只读取最终有序输出和允许商品事实
- [x] 6.2 校验文案响应不得修改坑位、图片、顺序、比例、裁剪或压缩
- [x] 6.3 文案 AI 失败时保留坑位与处理输出，并允许重试或人工录入
- [x] 6.4 人工修改 AI 文案后记录 `manual_override` 并要求再次确认
- [x] 6.5 将历史三页面 workflow state 映射为新两页面，不移动或删除历史文件
- [x] 6.6 历史 AI/规则草稿保持可读；显式重新编排或编辑后迁移为确定性/人工来源
- [x] 6.7 增加历史未确认草稿、已处理输出、文案待确认和已完成任务恢复测试
- [x] 6.8 新任务按八个可见阶段重新显示序号，内部 stage ID、授权引用和历史恢复不得依赖显示序号
- [x] 6.9 历史任务暂停在 `image_review` 时恢复到素材选择并保留已有决定与审计

## 7. Skill、文档与发布门禁

- [x] 7.1 更新 `SKILL.md`，明确素材选择内自动预检、确定性编排、人工边界和文案 AI 边界
- [x] 7.2 更新决策边界，移除独立 suitability 用户决定，将 `slots_copy/slot_plan` 默认模式改为确定性并删除坑位 AI 路径
- [x] 7.3 更新数据 Schema、操作指南、素材要求、错误处理和 `test_plan.md`
- [x] 7.4 更新恢复指令，确保 Agent 不再等待或领取坑位 AI 请求
- [x] 7.5 证明 dry-run、精确授权、生产确认和真实上传门禁按 stage ID 保持不变
- [x] 7.6 运行全量 Python、Node、前端语法、Skill validator、OpenSpec strict、锁文件和差异检查
- [x] 7.7 使用历史 session 验证独立适用性阶段与三页面迁移，并用可读新任务验证 1 张阻断、3 张放行、9 张生成 3 坑、人工调整、裁剪和 AI 文案
- [x] 7.8 保存浏览器验收证据和发布状态；本变更不得执行真实上传
