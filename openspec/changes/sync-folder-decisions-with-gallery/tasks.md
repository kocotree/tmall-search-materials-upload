## 1. 候选数据归属

- [x] 1.1 为 `confirmed_assets` 生成的每条图片候选补充稳定的 `folder_id` 和可审计 `folder_path`
- [x] 1.2 增加候选归属单元测试，覆盖同名、嵌套文件夹以及多来源目录
- [x] 1.3 实现历史候选缺少 `folder_id` 时的规范化最长父路径兜底和未关联标记

## 2. 文件夹二态交互

- [x] 2.1 从第三阶段文件夹决定控件中删除“待确认”，只保留“采用”和“排除该文件夹”
- [x] 2.2 将无决定、历史 `pending` 和新候选的页面派生状态统一为默认采用
- [x] 2.3 更新文件夹处理数量、卡片样式和说明文字，使其符合二态模型

## 3. 文件夹与画廊实时同步

- [x] 3.1 建立按 `(product_id, folder_id)` 从原始候选派生可见候选的前端状态函数
- [x] 3.2 在文件夹采用/排除变化时立即更新候选数量、30 张分页、当前页码和换批按钮
- [x] 3.3 排除文件夹时同步移除对应 `asset_decisions` 和 `license_decisions`
- [x] 3.4 在“已选素材”区域同步更新缩略图和数量，并显示本次自动取消的准确图片数量
- [x] 3.5 验证重新采用文件夹只恢复候选图片，不恢复此前取消的图片选择和授权

## 4. 后端一致性与兼容

- [x] 4.1 更新 `asset_matching` 输入规范化，接受二态决定且不再持久化 `pending`
- [x] 4.2 在后端提交边界按最终文件夹决定过滤矛盾的图片采用和授权记录
- [x] 4.3 增加历史任务兼容测试，覆盖缺少决定、`pending` 和缺少 `folder_id` 的结果
- [x] 4.4 增加恶意或旧客户端矛盾提交测试，确认被排除文件夹的图片无法进入下一阶段

## 5. 页面与流程验收

- [x] 5.1 增加前端静态和交互测试，验证页面不存在“待确认”选项
- [x] 5.2 增加浏览器验收：排除文件夹后候选、分页和已选素材立即同步
- [x] 5.3 增加浏览器验收：重新采用文件夹后候选恢复但旧选择不恢复
- [x] 5.4 运行完整 pytest、JavaScript 语法检查、Skill validator 和 OpenSpec validation

## 6. 文档更新

- [x] 6.1 更新 `SKILL.md` 和操作说明中的第三阶段文件夹决定及实时联动逻辑
- [x] 6.2 更新 `test_plan.md`，删除仍要求保留“待确认”的旧描述并补充二态同步测试
- [x] 6.3 重启本地交互服务，使用历史任务和新任务各完成一次第三阶段人工确认
